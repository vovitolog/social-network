import React from 'react';
import classes from 'src/components/Settings/Settings.module.css'

type DialogsProps = {

}

const Settings = (props: DialogsProps) => {
    return (
        <div>
            Settings
        </div>
    );
};

export default Settings;